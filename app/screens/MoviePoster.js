import React, { Component, PropTypes } from "react";
import { TouchableOpacity } from "react-native";

export default class MoviePoster extends Component {

    // static propTypes = {
    //     movie:PropTypes.object.isRequried,
    //     // onOpen: ReactPropTypes.func.isRequried
    // }

    render(){
        // const  {movie, movie: { title, genre }} = this.props ;
        return( 
           null
         );
    }
}